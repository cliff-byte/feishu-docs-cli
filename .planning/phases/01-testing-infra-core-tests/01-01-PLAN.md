---
phase: 01-testing-infra-core-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .c8rc.json
  - .gitignore
  - test/helpers/mock-fetch.ts
  - test/helpers/env-guard.ts
  - test/helpers/factory.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05

must_haves:
  truths:
    - "npm run test:coverage executes all tests and produces a coverage report"
    - "c8 + tsx pipeline produces accurate coverage data (or tsc fallback is configured)"
    - "test/helpers/ contains three reusable modules that all subsequent test plans import"
    - "coverage/ directory is gitignored"
  artifacts:
    - path: ".c8rc.json"
      provides: "Coverage configuration with thresholds"
      contains: "check-coverage"
    - path: "test/helpers/mock-fetch.ts"
      provides: "Fetch mock with call sequencing"
      exports: ["setupMockFetch", "jsonResponse", "tenantTokenResponse"]
    - path: "test/helpers/env-guard.ts"
      provides: "Environment variable isolation"
      exports: ["withCleanEnv", "withNoAuthEnv"]
    - path: "test/helpers/factory.ts"
      provides: "Test data factories"
      exports: ["makeAuthInfo", "makeUserAuthInfo", "makeGlobalOpts", "makeApiResponse"]
    - path: "package.json"
      provides: "test:coverage script"
      contains: "test:coverage"
  key_links:
    - from: "package.json"
      to: ".c8rc.json"
      via: "c8 reads config from .c8rc.json"
      pattern: "c8.*tsx"
    - from: "test/helpers/mock-fetch.ts"
      to: "globalThis.fetch"
      via: "replaces global fetch for testing"
      pattern: "globalThis\\.fetch"
---

<objective>
Set up the testing infrastructure for Phase 1: install c8 coverage tool, configure the coverage pipeline, create shared test helper modules, and add npm scripts.

Purpose: All subsequent test plans (02, 03, 04) depend on these helpers and the coverage pipeline. This must be done first.
Output: Working `npm run test:coverage` command, three helper modules in test/helpers/, .c8rc.json config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-testing-infra-core-tests/01-CONTEXT.md
@.planning/phases/01-testing-infra-core-tests/01-RESEARCH.md

<interfaces>
<!-- Key types the executor needs from the codebase -->

From src/types/index.ts:
```typescript
export interface AuthInfo {
  mode: AuthMode;
  appId: string;
  appSecret: string;
  useLark: boolean;
  userToken?: string;
  expiresAt?: number;
  refreshToken?: string;
}

export type AuthMode = "auto" | "user" | "tenant";

export interface GlobalOpts {
  auth: AuthMode;
  json: boolean;
  lark: boolean;
}

export interface ApiResponse<T = unknown> {
  code: number;
  msg?: string;
  data?: T;
  error?: Record<string, unknown>;
}
```

From test/fetch-binary.test.ts (existing mock pattern):
```typescript
// Manual globalThis.fetch replacement with call counting
const originalFetch = globalThis.fetch;
afterEach(() => { globalThis.fetch = originalFetch; });
function setupFetch(binaryResponse: Response) {
  globalThis.fetch = (async (..._args: unknown[]) => { ... }) as typeof fetch;
}
```

From test/scope-prompt.test.ts (existing env save/restore):
```typescript
function makeGlobalOpts(overrides: Partial<GlobalOpts> = {}): GlobalOpts {
  return { auth: "auto", json: false, lark: false, ...overrides };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install c8, configure coverage pipeline, add npm scripts</name>
  <files>package.json, .c8rc.json, .gitignore</files>
  <read_first>
    - package.json (current scripts and devDependencies)
    - .gitignore (current entries)
    - .planning/phases/01-testing-infra-core-tests/01-RESEARCH.md (lines 1-98, stack and config details)
  </read_first>
  <action>
    1. Install c8 as devDependency (per D-01):
       ```bash
       npm install -D c8
       ```

    2. Create `.c8rc.json` in project root with this exact content (per D-02, D-03):
       ```json
       {
         "all": true,
         "src": ["src"],
         "exclude": ["src/types/**", "dist/**", "test/**"],
         "reporter": ["html", "lcov", "text"],
         "report-dir": "coverage",
         "check-coverage": false,
         "lines": 80,
         "branches": 70,
         "functions": 80
       }
       ```
       NOTE: `check-coverage` is `false` for Phase 1 (per D-02: thresholds not enforced until Phase 2).

    3. Add `coverage/` to `.gitignore` (per D-03). Append the line `coverage/` under a `# Coverage reports` comment.

    4. Add `test:coverage` script to package.json scripts (per D-05):
       ```json
       "test:coverage": "c8 tsx --test test/*.test.ts"
       ```

    5. Verify the c8 + tsx pipeline (per TEST-02):
       Run `npm run test:coverage` and inspect output. Check that:
       - All existing tests pass
       - Coverage report is generated in `coverage/` directory
       - Spot-check: an untested file (e.g., src/commands/create.ts) should show LOW coverage (not 100%)

       If the coverage data is unreliable (all files show ~100%), switch to tsc fallback:
       ```json
       "test:coverage": "tsc && c8 node --test dist/**/*.test.js"
       ```
       Document which pipeline was chosen in the summary.

    6. Concurrency convention (per D-09, TEST-04): No file changes needed -- this is a convention documented in CONTEXT.md. All new test files in subsequent plans will use `{ concurrency: 1 }` on describe blocks that touch global state.
  </action>
  <verify>
    <automated>npm run test:coverage 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - .c8rc.json exists and contains `"check-coverage": false`
    - .c8rc.json contains `"reporter": ["html", "lcov", "text"]`
    - .c8rc.json contains `"lines": 80`
    - .gitignore contains `coverage/`
    - package.json scripts contains `"test:coverage"`
    - `npm run test:coverage` exits 0 and produces output in coverage/ directory
    - c8 devDependency appears in package.json
  </acceptance_criteria>
  <done>c8 installed, .c8rc.json configured, test:coverage script works, coverage/ is gitignored</done>
</task>

<task type="auto">
  <name>Task 2: Create test helper modules (mock-fetch, env-guard, factory)</name>
  <files>test/helpers/mock-fetch.ts, test/helpers/env-guard.ts, test/helpers/factory.ts</files>
  <read_first>
    - test/fetch-binary.test.ts (existing globalThis.fetch mock pattern -- reference implementation per D-04)
    - test/scope-prompt.test.ts (existing env var save/restore pattern and makeGlobalOpts factory)
    - src/types/index.ts (AuthInfo, GlobalOpts, ApiResponse type definitions)
    - .planning/phases/01-testing-infra-core-tests/01-RESEARCH.md (lines 117-294, helper module designs)
  </read_first>
  <action>
    Create `test/helpers/` directory with three modules (per D-07).

    **1. test/helpers/mock-fetch.ts** (per D-04, D-06):
    - Export `setupMockFetch(opts)` that:
      - Saves `globalThis.fetch`, replaces it with a mock that returns responses in order
      - Tracks all calls in a `calls` array of `{ url, init }` objects
      - Has `strictCount` option (default true) that throws if more calls than responses
      - Returns `{ calls, restore }` where `restore()` puts back original fetch
    - Export `jsonResponse(body, status?)` that creates a `new Response(JSON.stringify(body), { status, headers: { "content-type": "application/json" } })`
    - Export `tenantTokenResponse(token?)` that returns `jsonResponse({ code: 0, tenant_access_token: token || "t-mock-token" })`
    - Use the exact API signatures from RESEARCH.md Pattern 1 (lines 124-191)
    - Do NOT use `t.mock.method()` for fetch mocking -- use direct `globalThis.fetch` replacement (per D-04)

    **2. test/helpers/env-guard.ts**:
    - Define `AUTH_ENV_KEYS` constant array: `["FEISHU_USER_TOKEN", "FEISHU_APP_ID", "FEISHU_APP_SECRET", "FEISHU_REDIRECT_URI", "FEISHU_OAUTH_PORT"]`
    - Export `withCleanEnv(vars, fn)` that saves specified env vars, sets new values, runs fn, restores originals in finally block
    - Export `withNoAuthEnv(fn)` that clears all FEISHU_* auth env vars for the duration
    - Use the exact implementation from RESEARCH.md Pattern 2 (lines 200-253)

    **3. test/helpers/factory.ts**:
    - Import types from `../../src/types/index.js` (with .js extension per ESM convention)
    - Export `makeAuthInfo(overrides?)` -- default: `{ mode: "tenant", appId: "cli_test_id", appSecret: "cli_test_secret", useLark: false }`
    - Export `makeUserAuthInfo(overrides?)` -- extends makeAuthInfo with `mode: "user"`, `userToken: "u-mock-user-token"`, `expiresAt: Date.now() + 3600_000`, `refreshToken: "rt-mock-refresh-token"`
    - Export `makeGlobalOpts(overrides?)` -- default: `{ auth: "auto", json: false, lark: false }`
    - Export `makeApiResponse(overrides?)` -- default: `{ code: 0, msg: "success", data: {} }`
    - Use the exact implementation from RESEARCH.md Pattern 3 (lines 261-293)
    - Per D-08: do NOT migrate existing inline factories from other test files

    All files must:
    - Use ESM `import`/`export` syntax
    - Use `.js` extensions on relative imports
    - Follow project naming conventions (camelCase functions, PascalCase types)
    - Include JSDoc comments on exported functions
  </action>
  <verify>
    <automated>npx tsx --test test/*.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/helpers/mock-fetch.ts exists and contains `export function setupMockFetch`
    - test/helpers/mock-fetch.ts contains `export function jsonResponse`
    - test/helpers/mock-fetch.ts contains `export function tenantTokenResponse`
    - test/helpers/env-guard.ts exists and contains `export async function withCleanEnv`
    - test/helpers/env-guard.ts contains `export async function withNoAuthEnv`
    - test/helpers/factory.ts exists and contains `export function makeAuthInfo`
    - test/helpers/factory.ts contains `export function makeUserAuthInfo`
    - test/helpers/factory.ts contains `export function makeGlobalOpts`
    - test/helpers/factory.ts contains `export function makeApiResponse`
    - factory.ts imports from `../../src/types/index.js` (with .js extension)
    - All existing tests still pass (exit code 0 from `npx tsx --test test/*.test.ts`)
  </acceptance_criteria>
  <done>Three helper modules in test/helpers/ are created with documented APIs, existing tests unaffected</done>
</task>

<task type="auto">
  <name>Task 3: Validate helpers with a smoke test</name>
  <files>test/helpers-smoke.test.ts</files>
  <read_first>
    - test/helpers/mock-fetch.ts (just created)
    - test/helpers/env-guard.ts (just created)
    - test/helpers/factory.ts (just created)
  </read_first>
  <action>
    Create `test/helpers-smoke.test.ts` -- a lightweight test file that validates the three helper modules work correctly. This ensures the helpers are importable and functional before downstream plans depend on them.

    Tests to include:
    1. **mock-fetch**: Call `setupMockFetch` with one `jsonResponse`, invoke `globalThis.fetch`, verify `calls` array has one entry with correct URL, call `restore()`, verify `globalThis.fetch` is restored to original
    2. **mock-fetch strict mode**: Call `setupMockFetch` with zero responses, invoke `globalThis.fetch`, assert it throws with "Unexpected fetch call"
    3. **mock-fetch tenantTokenResponse**: Verify `tenantTokenResponse()` returns a Response with status 200 and body containing `tenant_access_token`
    4. **env-guard**: Call `withCleanEnv({ FEISHU_APP_ID: "test-id" }, ...)`, verify env var is set inside callback, verify it's restored after
    5. **withNoAuthEnv**: Set `FEISHU_APP_ID` before, call `withNoAuthEnv(...)`, verify it's undefined inside, verify restored after
    6. **factory**: Call each factory function, verify returned objects have expected shape (mode, appId, etc.)

    Use `{ concurrency: 1 }` on the top-level describe block (per D-09, since these tests touch globalThis.fetch and process.env).
  </action>
  <verify>
    <automated>npx tsx --test test/helpers-smoke.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/helpers-smoke.test.ts exists and contains `describe("test helpers"`
    - test/helpers-smoke.test.ts contains `{ concurrency: 1 }`
    - test/helpers-smoke.test.ts imports from `./helpers/mock-fetch.js`
    - test/helpers-smoke.test.ts imports from `./helpers/env-guard.js`
    - test/helpers-smoke.test.ts imports from `./helpers/factory.js`
    - `npx tsx --test test/helpers-smoke.test.ts` exits 0
    - `npm run test:coverage` still exits 0 (all tests pass including smoke test)
  </acceptance_criteria>
  <done>All three helper modules verified working via smoke test, full test suite passes</done>
</task>

</tasks>

<verification>
1. `npm run test:coverage` exits 0 and produces coverage/ directory with HTML report
2. `ls test/helpers/` shows mock-fetch.ts, env-guard.ts, factory.ts
3. `npx tsx --test test/helpers-smoke.test.ts` exits 0
4. All 16+ existing tests continue to pass
5. .c8rc.json exists with correct configuration
6. .gitignore contains `coverage/`
</verification>

<success_criteria>
- c8 installed and coverage pipeline verified accurate (TEST-01, TEST-02)
- Three helper modules created and smoke-tested (TEST-03)
- Concurrency convention established (TEST-04)
- test:coverage script available (TEST-05)
- All existing tests unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/01-testing-infra-core-tests/01-01-SUMMARY.md`
</output>
